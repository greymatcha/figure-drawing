name: Deployment develop windows
on:
  push:
    branches: [develop]

  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      # - uses: subosito/flutter-action@v2
      #   with:
      #     channel: 'beta'
      # - run: flutter pub get
      # - run: flutter analyze
      # - run: flutter config --enable-windows-desktop
      # - run: flutter build windows
      - run: |
          $pwd
      - run: |
          ls
